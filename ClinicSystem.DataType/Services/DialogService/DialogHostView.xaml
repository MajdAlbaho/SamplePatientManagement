<UserControl x:Class="ClinicSystem.DataType.Services.DialogService.DialogHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClinicSystem.DataType.Services.DialogService"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Background="Black"
                Opacity=".6" />
        <Border VerticalAlignment="Center"
                Background="WhiteSmoke">
            <ContentControl Content="{Binding Content,UpdateSourceTrigger=PropertyChanged}"
                            MinHeight="100"
                            HorizontalAlignment="Center"
                            Margin="50 20" />
        </Border>
    </Grid>
</UserControl>
